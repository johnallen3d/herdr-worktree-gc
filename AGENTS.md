# Agent instructions

- Always declare project tools in `mise.toml` and run them through Mise.
- Do not create or use a `scripts/` directory.
- Do not use Python.
- Close Fizzy cards only when John explicitly asks you to close them.
